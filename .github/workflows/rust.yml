name: Rust

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

env:
  CARGO_TERM_COLOR: always
  RUST_LOG: trace

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: Build `netstack`
      run: cargo build --verbose
    - name: Run tests for `netstack`
      run: cargo test --verbose
    - name: Build `netstack_tcp`
      run: cargo build --verbose --manifest-path netstack_tcp/Cargo.toml
    - name: Run tests for `netstack_tcp`
      run: cargo test --verbose --manifest-path netstack_tcp/Cargo.toml
